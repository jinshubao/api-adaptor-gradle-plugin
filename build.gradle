group 'shangying'
version '1.0.0'

apply plugin: 'groovy'
apply plugin: 'maven'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()

    testCompile('junit:junit:4.12')
}

tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}
